#include <iostream>
#include <fstream>

using namespace std;

int main(int argc, char* argv[]){
    ifstream input, apsp, apasp;
    
    input.open(argv[1]);
    apsp.open(argv[2]);
    apasp.open(argv[3]);
    
    

    return 0;
}